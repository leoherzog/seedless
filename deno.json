{
  "imports": {
    "trystero/nostr": "https://cdn.jsdelivr.net/npm/@trystero-p2p/nostr/+esm"
  },
  "tasks": {
    "test": "deno test --allow-read",
    "test:watch": "deno test --allow-read --watch",
    "test:coverage": "rm -rf coverage && deno test --allow-read --coverage=coverage && deno coverage coverage"
  }
}
